name: Publish to NPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  
jobs:
  # test:
  #   name: Build, Test, and Validate
  #   runs-on: ubuntu-latest
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
      
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20.x'
  #         cache: 'yarn'
      
  #     - name: Install dependencies
  #       run: yarn install --immutable
      
  #     - name: Build all packages
  #       run: yarn build
      
  #     - name: Run linter
  #       run: yarn lint
      
  #     - name: Type check
  #       run: yarn typecheck
      
  #     - name: Run tests
  #       run: yarn test

  publish:
    name: Publish Packages
    # needs: test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'yarn'
          registry-url: 'https://registry.npmjs.org'

      - name: Update npm to latest
        run: npm install -g npm@latest

      - name: Verify npm version
        run: npm --version
      
      - name: Install dependencies
        run: yarn install --immutable
      
      - name: Build all packages
        run: yarn build
      
      - name: Publish @apiquest/types
        working-directory: ./packages/types
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/fracture
        working-directory: ./packages/fracture
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/plugin-http
        working-directory: ./packages/plugin-http
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/plugin-auth
        working-directory: ./packages/plugin-auth
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/plugin-vault-file
        working-directory: ./packages/plugin-vault-file
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/plugin-graphql
        working-directory: ./packages/plugin-graphql
        run: npm publish --provenance --access public
      
      - name: Publish @apiquest/plugin-sse
        working-directory: ./packages/plugin-sse
        run: npm publish --provenance --access public
